TODO
• Add test-data for remaining schemas: Customer & Booking (opt Email)
• Create instructions for executing
	➢ SET GLOBAL local_infile = 1;
	➢ SHOW VARIABLES LIKE 'local_infile';
	➢ Run as administrator
	➢ Proper folder
	➢ Cleanup script
• Organize script naming convention and placement
• Improve selector
• Add readme for SuperAdmin 2

I took the liberty of creating a directory for all non-code-related matters - /Scripts

I also added the mockups and project requirements, as I found myself using them a lot so its gonna be convenient to have
have them - we can always get rid of them later.

In the /Scripts/DB dir you will find a .sql (one for now) file with the DDL script for creating the database tables.
Don't run it for now as we first have to agree on the DB structure (deleting or renaming tables/columns with so many
FK's is a hassle) and also it is written in TSQL (#uck microsoft but im used to their dialect), so we will have to rewrite it 
to the MySQL dialect.

In the Scripts/PNG you will find the .png files of the most important schemas: Booking, Car, Customer, Email
Since there are a lot of FK's defined in the tables, I couldn't nicely create the diagram of the Booking schema, but I think 
you will manage.

In the Scripts/DB/DDL/TSQL_DDL.sql script you will find a lot of TODO's scattered around the code, these were the points
where something wasn't clear to me or required later attention - I recommend Ctrl + F'ing through them all.

Should you have any questions or comments, please DO hestitate to contact me. 